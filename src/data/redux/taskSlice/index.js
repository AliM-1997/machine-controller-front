import { createSlice } from "@reduxjs/toolkit";

const taskSlice = createSlice({ initialState, name: "", reducers });
export const {} = taskSlice.actions;
export const taskReducer = taskSlice.reducer;
