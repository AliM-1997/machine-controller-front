import React from "react";
import "./style.css";
const DropButton = ({
  options,
  onSelect,
  placeHolder,
  backgroundColor,
  width,
  textColor,
  leftIcon,
  rightIcon,
  iconColor,
}) => {
  return <div></div>;
};

export default DropButton;
